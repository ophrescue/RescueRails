<%= form_for [@adopter, @adopter.comments.build], data: {remote: true}, :html => {novalidate: true, :id => "new_comment"} do |f| %>
  <%= f.text_area :content, :class => "input-xxlarge", :rows => "5" %>
  <%= f.submit "Post", class:"btn btn-primary", id: "comment_submit" %>
<% end %>
