<li class="media">
    <div class="media-body pb-1 mb-3">
    <h5 class="mt-0 mb-1"><%= link_to(bulletin.title.titleize, bulletin) %></h5>
    <p class="small text-muted">Posted by <%= bulletin.user.name.titleize %> <%= time_ago_in_words(bulletin.created_at) %> ago.</p>
    </div>
</li>
