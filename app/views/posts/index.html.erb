<div class="container">
  <div class="row title_row">
    <div class="col">
      <h1><%= params[:type].pluralize %></h1>
    </div>
  </div>
  <% if current_user.admin? %>
  <div class="row">
    <div class="col pb-5">
      <%= link_to "Add a #{params[:type]}", new_polymorphic_path(params[:type].downcase), class: "btn btn-primary" %>
    </div>
  </div>
  <% end %>

      <%= render partial: 'post', collection: @posts %>

</div>
