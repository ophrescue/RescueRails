<div class="container">
    <div class="row">
        <div class="col-12">
            <h4>Edit <%= @treatment.name %> for <%= @treatable.name %> </h4>
            <p>
                <em><%= @treatment.recommendation %></em>
            </p>
                <%= render 'form'%>
        </div>
    </div>
</div>
