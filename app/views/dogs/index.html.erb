<h1><%= @title %></h1>
<% if can_edit_dogs? %>
<p>
<%= link_to "Add a Dog", new_dog_path %>
</p>
<% end %>

<% if can_edit_dogs? %>
	<%= render 'manager' %>
<% else %>
	<div class="row">
		<div class="span16">
			<ul class="media-grid">
				<%= render @dogs %>
			</ul>
		</div>
	</div>
<% end %>